<nav class="navbar navbar-default">
  <div class="container-fluid">
    <a class="navbar-brand" href="/"><span class="glyphicon glyphicon-heart"></span> madewithlove</a>
    <div class="navbar-text navbar-right">
      <% if current_user %>
        <a data-toggle="popover" id="profile" href="/users/<%= current_user.id %>" class="navbar-link">
          <!-- <img  -->
            <% if current_user.updated_image_file_name %>
              <%= image_tag current_user.updated_image.url(:large), class:"navbar-avatar pull-right" %>
            <% elsif current_user.chef_avatar %>
              <%= image_tag current_user.chef_avatar,class:"navbar-avatar pull-right" %>
              <!-- src="<%= current_user.chef_avatar %>" -->
            <% else %>
            <%= image_tag current_user.avatar, class:"navbar-avatar pull-right" %>
             <!-- src="<%= current_user.avatar %>" -->
            <% end %>
        <!-- </a> -->
      <% else %>
        <a href="/login" class="" data-toggle="modal" data-target="#login">Login</a>
        <a href="/register" class="" data-toggle="modal" data-target="#sign-up">Sign Up</a>
      <% end %>
    </div>

    <% if current_user %>
      <div id="profile-popover" class="hidden">
        <%= link_to "Edit Profile", edit_user_path(current_user) %>
        <a href="/logout">Logout</a>
      </div>
    <% end %>
  </div>
</nav>

<!-- Render the modals -->
<%= render 'layouts/modals/login' %>
<%= render 'layouts/modals/signup' %>
